export enum Status {
    Deleted = 0,
    Active = 1,
    InActive = 2,
    Locked = 3,
    Flagged = 4,
    UnFlagged = 5,
    ClosureRequested = 6,
    Blocked = 7,
    Hold = 8,
    Expired = 9,
    Suspend = 10
}

export enum UserStatus {
    Active = 1,
    InActive = 2,
}